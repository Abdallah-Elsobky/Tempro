import Foundation

/// APIEndpoints defines endpoints for the weathercast services.
struct APIEndpoints {
    // TODO: Sprint 3 — Define base URL and endpoint paths for weather queries
    static let baseURL = "https://api.weatherapi.com/v1"
}
