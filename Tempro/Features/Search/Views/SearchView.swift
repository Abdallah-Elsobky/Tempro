import SwiftUI

struct SearchView: View {
    @StateObject private var viewModel = SearchViewModel()
    
    var body: some View {
        // TODO: Sprint 4 — implement city search textfield and list
        VStack {
            Text("Search View Stub")
                .padding()
        }
    }
}

#Preview {
    SearchView()
}
